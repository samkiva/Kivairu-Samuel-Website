'use client';

import * as React from 'react';

export function AnalyticsProvider({ children }: { children: React.ReactNode }) {
  // Placeholder for Vercel Analytics, Google Analytics, or Plausible
  return <>{children}</>;
}
